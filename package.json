{
  "name": "diployed-chat-app",
  "version": "1.0.0",
  "description": "this my what app clone project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build  && ../socket && npm install && npm start",
    "start": "node backend/index.js && node socket/index.js"
  },
  "author": "prashant bokade",
  "license": "ISC"
}
